#ifndef HUTILS    // To make sure you don't declare the function more than once by including the header multiple times.
#define HUTILS

bool** create2darrB(int rows, int cols);
int fact(int n);

#endif